<ui:FluentWindow
    x:Class="VCenterMigrationTool.Views.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tray="http://schemas.lepo.co/wpfui/2022/xaml/tray"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:VCenterMigrationTool.ViewModels"
    xmlns:helpers="clr-namespace:VCenterMigrationTool.Helpers"
    mc:Ignorable="d"
    Title="{Binding ApplicationTitle, Mode=OneWay}"    Width="1100"
    Height="650"
    d:DataContext="{d:DesignInstance viewmodels:MainWindowViewModel, IsDesignTimeCreatable=True}"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica"
    WindowCornerPreference="Round"
    WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- vCenter Connection Banner -->
        <Border Grid.Row="1" 
                Background="{DynamicResource ControlFillColorInputActiveBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorSecondaryBrush}"
                BorderThickness="0,0,0,1"
                Padding="20,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Source vCenter Connection -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="Server24" Margin="0,0,8,0"/>
                    <TextBlock Text="Source:" 
                              FontWeight="SemiBold"
                              VerticalAlignment="Center"
                              Margin="0,0,8,0"/>
                </StackPanel>
                
                <ComboBox Grid.Column="1" 
                         ItemsSource="{Binding SourceProfiles}"
                         SelectedItem="{Binding SelectedSourceProfile}"
                         DisplayMemberPath="Name"
                         MinWidth="150"
                         Margin="0,0,10,0"/>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Border Background="{Binding SourceConnectionBackgroundBrush}"
                           BorderBrush="{Binding SourceConnectionBorderBrush}"
                           BorderThickness="1"
                           CornerRadius="3"
                           Padding="8,4"
                           Margin="0,0,8,0">
                        <TextBlock Text="{Binding SourceConnectionStatus}"
                                  FontSize="11"
                                  Foreground="{Binding SourceConnectionTextBrush}"/>
                    </Border>
                    <ui:Button Content="Connect" 
                              Command="{Binding ConnectSourceCommand}"
                              IsEnabled="{Binding CanConnectSource}"
                              Appearance="Primary"
                              FontSize="11"
                              Padding="12,6"
                              Margin="0,0,4,0"/>
                    <ui:Button Content="×" 
                              Command="{Binding DisconnectSourceCommand}"
                              IsEnabled="{Binding CanDisconnectSource}"
                              Appearance="Danger"
                              FontSize="11"
                              Padding="8,6"/>
                </StackPanel>
                
                <!-- Separator -->
                <Border Grid.Column="3" 
                       Width="1" 
                       Background="{DynamicResource ControlStrokeColorSecondaryBrush}"
                       Margin="10,4"/>
                
                <!-- Target vCenter Connection -->
                <StackPanel Grid.Column="4" Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="ServerMultiple20" Margin="0,0,8,0"/>
                    <TextBlock Text="Target:" 
                              FontWeight="SemiBold"
                              VerticalAlignment="Center"
                              Margin="0,0,8,0"/>
                </StackPanel>
                
                <ComboBox Grid.Column="5" 
                         ItemsSource="{Binding TargetProfiles}"
                         SelectedItem="{Binding SelectedTargetProfile}"
                         DisplayMemberPath="Name"
                         MinWidth="150"
                         Margin="0,0,10,0"/>
                
                <StackPanel Grid.Column="6" Orientation="Horizontal">
                    <Border Background="{Binding TargetConnectionBackgroundBrush}"
                           BorderBrush="{Binding TargetConnectionBorderBrush}"
                           BorderThickness="1"
                           CornerRadius="3"
                           Padding="8,4"
                           Margin="0,0,8,0">
                        <TextBlock Text="{Binding TargetConnectionStatus}"
                                  FontSize="11"
                                  Foreground="{Binding TargetConnectionTextBrush}"/>
                    </Border>
                    <ui:Button Content="Connect" 
                              Command="{Binding ConnectTargetCommand}"
                              IsEnabled="{Binding CanConnectTarget}"
                              Appearance="Primary"
                              FontSize="11"
                              Padding="12,6"
                              Margin="0,0,4,0"/>
                    <ui:Button Content="×" 
                              Command="{Binding DisconnectTargetCommand}"
                              IsEnabled="{Binding CanDisconnectTarget}"
                              Appearance="Danger"
                              FontSize="11"
                              Padding="8,6"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <ui:NavigationView helpers:WindowScrollBehavior.EnableGlobalScroll="True"
                           x:Name="RootNavigation"
                           Grid.Row="2"
                           MenuItemsSource="{Binding NavigationItems, Mode=OneWay}"
                           FooterMenuItemsSource="{Binding NavigationFooter, Mode=OneWay}"
                           IsBackButtonVisible="Visible"
                           IsPaneOpen="True">  
        <ui:NavigationView.Header>
            <ui:BreadcrumbBar
                Margin="42,32,0,0"
                FontSize="28"
                FontWeight="DemiBold" />
        </ui:NavigationView.Header>
        </ui:NavigationView>
        <ui:TitleBar 
            Grid.Row="0" 
            Title="{Binding ApplicationTitle}"
            Icon="pack://application:,,,/Assets/Gemini_Generated_Image_b1i2b6b1i2b6b1i2.png" />
        <tray:NotifyIcon
            Grid.Row="0"
            FocusOnLeftClick="True"
            Icon="pack://application:,,,/Assets/Gemini_Generated_Image_b1i2b6b1i2b6b1i2.png"
            MenuOnRightClick="True"
            TooltipText="{Binding ApplicationTitle}">
            <tray:NotifyIcon.Menu>
                <ContextMenu ItemsSource="{Binding ViewModel.TrayMenuItems, Mode=OneWay}" />
            </tray:NotifyIcon.Menu>
        </tray:NotifyIcon>
    </Grid>
</ui:FluentWindow>
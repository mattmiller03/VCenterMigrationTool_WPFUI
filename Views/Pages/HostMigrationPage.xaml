<Page x:Class="VCenterMigrationTool.Views.Pages.HostMigrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:viewmodels="clr-namespace:VCenterMigrationTool.ViewModels"
      xmlns:helpers="clr-namespace:VCenterMigrationTool.Helpers"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance Type=viewmodels:HostMigrationViewModel}"
      Title="HostMigrationPage">
    <Page.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <helpers:BoolToParameterConverter x:Key="BoolToParameterConverter" />
        <helpers:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
    </Page.Resources>
    <Grid Margin="42">
        <TextBlock Grid.Row="0" FontSize="24" FontWeight="Medium" Text="ESXi Host Migration" />

        <TreeView ItemsSource="{Binding SourceTopology}" />
        <ComboBox ItemsSource="{Binding TargetClusters}" SelectedItem="{Binding SelectedTargetCluster}" />
        <TextBlock Visibility="{Binding SelectedTargetCluster, Converter={StaticResource NullToVisibilityConverter}}" />
        <Button Command="{Binding MigrateHostsCommand}" IsEnabled="{Binding IsBusy, Converter={StaticResource BoolToParameterConverter}, ConverterParameter=inverse}"/>
        <ui:ProgressRing Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}" />
        <ui:TextBox Text="{Binding LogOutput, Mode=OneWay}" />
    </Grid>
</Page>
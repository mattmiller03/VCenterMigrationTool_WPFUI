<Page
    x:Class="VCenterMigrationTool.Views.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:VCenterMigrationTool.ViewModels"
    xmlns:helpers="clr-namespace:VCenterMigrationTool.Helpers"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewmodels:DashboardViewModel, IsDesignTimeCreatable=False}"
    Title="DashboardPage"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Grid Margin="42">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            FontSize="24"
            FontWeight="Medium"
            Text="Export / Import vCenter Configuration" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,20,0,10">
            <ui:Button
                Content="Export vDS &amp; Permissions"
                Appearance="Primary"
                Icon="ArrowUpload24"
                Command="{Binding ViewModel.ExportConfigurationCommand}"
                IsEnabled="{Binding ViewModel.IsBusy, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=inverse}"/>

            <ui:Button
                Content="Import vDS &amp; Permissions"
                Appearance="Secondary"
                Icon="ArrowDownload24"
                Command="{Binding ViewModel.ImportConfigurationCommand}"
                Margin="10,0,0,0"
                IsEnabled="{Binding ViewModel.IsBusy, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=inverse}"/>

            <ui:ProgressRing 
                Margin="10,0,0,0" 
                IsIndeterminate="True" 
                Visibility="{Binding ViewModel.IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>

        <ui:TextBox
            Grid.Row="2"
            Text="{Binding ViewModel.ScriptOutput, Mode=OneWay}"
            IsReadOnly="True"
            AcceptsReturn="True"
            VerticalScrollBarVisibility="Auto"
            FontFamily="Consolas"/>
    </Grid>
</Page>
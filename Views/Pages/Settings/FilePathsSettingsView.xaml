<UserControl x:Class="VCenterMigrationTool.Views.Pages.Settings.FilePathsSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:viewmodels="clr-namespace:VCenterMigrationTool.ViewModels.Settings"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:FilePathsSettingsViewModel, IsDesignTimeCreatable=False}">
    <StackPanel>
        <TextBlock FontSize="20" FontWeight="Medium" Text="File Paths" />
        <TextBlock Margin="0,12,0,0" Text="Log File Location" />
        <Grid Margin="0,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:TextBox Grid.Column="0" Text="{Binding LogPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <ui:Button Grid.Column="2" Command="{Binding BrowseLogPathCommand}" ToolTip="Browse for folder" Icon="{ui:SymbolIcon FolderOpen24}" />
        </Grid>
        <TextBlock Margin="0,12,0,0" Text="Export/Backup Location" />
        <Grid Margin="0,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:TextBox Grid.Column="0" Text="{Binding ExportPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <ui:Button Grid.Column="2" Command="{Binding BrowseExportPathCommand}" ToolTip="Browse for folder" Icon="{ui:SymbolIcon FolderOpen24}" />
        </Grid>
        <ui:Button Content="Save Changes" Icon="{ui:SymbolIcon Save24}" Appearance="Primary" Margin="0,20,0,0" Command="{Binding SaveChangesCommand}" />
    </StackPanel>
</UserControl>
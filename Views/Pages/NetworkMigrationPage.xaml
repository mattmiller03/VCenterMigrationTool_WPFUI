<Page
    x:Class="VCenterMigrationTool.Views.Pages.NetworkMigrationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:VCenterMigrationTool.ViewModels"
    xmlns:helpers="clr-namespace:VCenterMigrationTool.Helpers"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewmodels:NetworkMigrationViewModel, IsDesignTimeCreatable=False}"
    Title="NetworkMigrationPage"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Page.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <Grid Margin="42">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="24" FontWeight="Medium" Text="Network Migration" />

        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <ui:CardControl Grid.Column="0">
                <StackPanel>
                    <TextBlock Text="Source vCenter Networks" FontWeight="Medium" />
                    <TreeView ItemsSource="{Binding VSwitches}" Margin="0,10,0,0">
                        <TreeView.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding PortGroups}">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                                <HierarchicalDataTemplate.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" />
                                            <TextBlock Grid.Column="1" Text="{Binding Name}" Margin="5,0" />
                                            <TextBlock Grid.Column="2" Text="{Binding Type}" Foreground="Gray" />
                                        </Grid>
                                    </DataTemplate>
                                </HierarchicalDataTemplate.ItemTemplate>
                            </HierarchicalDataTemplate>
                        </TreeView.ItemTemplate>
                    </TreeView>
                </StackPanel>
            </ui:CardControl>

            <ui:CardControl Grid.Column="2">
                <StackPanel>
                    <TextBlock Text="Migration Options" FontWeight="Medium" />
                    <TextBlock Text="Target Cluster" Margin="0,10,0,5" />
                    <ComboBox ItemsSource="{Binding TargetClusters}" SelectedItem="{Binding SelectedTargetCluster}" DisplayMemberPath="Name" />
                    <TextBlock Text="Target vSwitch" Margin="0,10,0,5" />
                    <ComboBox ItemsSource="{Binding TargetVSwitches}" SelectedItem="{Binding SelectedTargetVSwitch}" DisplayMemberPath="Name" />

                    <ui:Button Content="Migrate Selected Networks" Command="{Binding MigrateNetworksCommand}" Margin="0,20,0,0" Appearance="Primary" />

                    <ProgressBar Value="{Binding MigrationProgress}" Maximum="100" Margin="0,10,0,0" Visibility="{Binding IsMigrating, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <TextBlock Text="{Binding MigrationStatus}" Margin="0,5,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                </StackPanel>
            </ui:CardControl>
        </Grid>
    </Grid>
</Page>
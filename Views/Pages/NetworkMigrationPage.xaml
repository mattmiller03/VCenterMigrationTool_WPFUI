<Page
    x:Class="VCenterMigrationTool.Views.Pages.NetworkMigrationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:VCenterMigrationTool.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewmodels:NetworkMigrationViewModel, IsDesignTimeCreatable=False}"
    Title="NetworkMigrationPage"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Grid Margin="42">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="350" />
        </Grid.ColumnDefinitions>

        <GroupBox Grid.Column="0" Header="Select Network Objects to Migrate">
            <TreeView ItemsSource="{Binding ViewModel.NetworkTopology}">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding VSwitches}">
                        <TextBlock Text="{Binding Name}" FontWeight="Medium" />
                        <HierarchicalDataTemplate.ItemTemplate>
                            <HierarchicalDataTemplate ItemsSource="{Binding PortGroups}">
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Name}" Margin="5,0,0,0"/>
                                    <TextBlock Text="{Binding Type, StringFormat='({0})'}" Margin="5,0,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                                <HierarchicalDataTemplate.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="20,0,0,0">
                                            <CheckBox IsChecked="{Binding IsSelected}" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding Name}" Margin="5,0,0,0"/>
                                            <TextBlock Text="{Binding VlanId, StringFormat='- VLAN {0}'}" Margin="5,0,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </HierarchicalDataTemplate.ItemTemplate>
                            </HierarchicalDataTemplate>
                        </HierarchicalDataTemplate.ItemTemplate>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>
        </GroupBox>

        <StackPanel Grid.Column="2">
            <TextBlock Text="Actions" FontSize="16" FontWeight="Medium" />
            <ui:Button 
                Content="Load Network Data" 
                Command="{Binding ViewModel.LoadNetworkDataCommand}"
                Icon="ArrowSync24"
                Margin="0,10,0,0"/>
            <ui:Button 
                Content="Start Network Migration" 
                Command="{Binding ViewModel.MigrateNetworkCommand}"
                Appearance="Primary"
                Icon="ArrowSwap24"
                Margin="0,10,0,0"
                IsEnabled="{Binding ViewModel.IsBusy, Converter={StaticResource BoolToParameterConverter}, ConverterParameter=inverse}"/>
            <ui:ProgressRing Margin="0,10,0,20" IsIndeterminate="True" Visibility="{Binding ViewModel.IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <TextBlock Text="Log Output" FontSize="16" FontWeight="Medium" />
            <ui:TextBox
                Text="{Binding ViewModel.LogOutput, Mode=OneWay}"
                IsReadOnly="True"
                AcceptsReturn="True"
                VerticalScrollBarVisibility="Auto"
                FontFamily="Consolas"
                Height="400"
                Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Page>
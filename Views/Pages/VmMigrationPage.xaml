<Page
    x:Class="VCenterMigrationTool.Views.Pages.VmMigrationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:VCenterMigrationTool.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewmodels:VmMigrationViewModel, IsDesignTimeCreatable=False}"
    Title="VmMigrationPage"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Grid Margin="42">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="200" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="24" FontWeight="Medium" Text="Virtual Machine Migration" />

        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="300" />
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Select VMs to Migrate (from Source vCenter)">
                <DataGrid ItemsSource="{Binding ViewModel.SourceVms}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn>
                            <DataGridTemplateColumn.Header>
                                <CheckBox />
                            </DataGridTemplateColumn.Header>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="VM Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Power State" Binding="{Binding PowerState}" Width="Auto"/>
                        <DataGridTextColumn Header="Host" Binding="{Binding EsxiHost}" Width="*"/>
                        <DataGridTextColumn Header="Datastore" Binding="{Binding Datastore}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <GroupBox Grid.Column="2" Header="Select Target (in Target vCenter)">
                <StackPanel>
                    <TextBlock Text="Target Host" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding ViewModel.TargetHosts}" SelectedItem="{Binding ViewModel.SelectedTargetHost}" DisplayMemberPath="Name" Margin="0,0,0,15"/>
                    <TextBlock Text="Target Datastore" Margin="0,0,0,5"/>
                    <ComboBox ItemsSource="{Binding ViewModel.TargetDatastores}" SelectedItem="{Binding ViewModel.SelectedTargetDatastore}" DisplayMemberPath="Name"/>
                </StackPanel>
            </GroupBox>
        </Grid>

        <StackPanel Grid.Row="2" Margin="0,20,0,10">
            <ui:Button 
                Content="Migrate Selected VMs"
                Command="{Binding ViewModel.MigrateVmsCommand}"
                Appearance="Primary"
                Icon="ArrowSwap24"
                IsEnabled="{Binding ViewModel.IsBusy, Converter={StaticResource BoolToParameterConverter}, ConverterParameter=inverse}"/>
            <ui:ProgressRing Margin="0,10,0,0" IsIndeterminate="True" Visibility="{Binding ViewModel.IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </StackPanel>

        <ui:TextBox
            Grid.Row="3"
            Text="{Binding ViewModel.LogOutput, Mode=OneWay}"
            IsReadOnly="True"
            AcceptsReturn="True"
            VerticalScrollBarVisibility="Auto"
            FontFamily="Consolas"/>
    </Grid>
</Page>
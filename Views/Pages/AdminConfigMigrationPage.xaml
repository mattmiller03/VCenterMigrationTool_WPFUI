<Page
    x:Class="VCenterMigrationTool.Views.Pages.AdminConfigMigrationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:VCenterMigrationTool.ViewModels"
    xmlns:helpers="clr-namespace:VCenterMigrationTool.Helpers"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewmodels:AdminConfigMigrationViewModel, IsDesignTimeCreatable=False}"
    Title="AdminConfigMigrationPage"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Page.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <helpers:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
        <helpers:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        <helpers:BoolToColorConverter x:Key="BoolToColorConverter"/>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Disabled"
                  PanningMode="VerticalOnly"
                  CanContentScroll="True">
        <Grid Margin="42">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" MinHeight="400"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Admin Configuration Migration" 
                               FontSize="28" 
                               FontWeight="SemiBold"/>
                    <TextBlock Text="Migrate roles, permissions, folders, tags, certificates, and administrative configurations" 
                               Opacity="0.7"
                               FontSize="14"
                               Margin="0,5,0,0"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <ui:Button Grid.Column="1" 
                          Content="Refresh" 
                          Icon="{ui:SymbolIcon ArrowSync24}"
                          Command="{Binding RefreshDataCommand}"
                          Appearance="Secondary"
                          VerticalAlignment="Center"
                          Padding="15,8"/>
            </Grid>

            <!-- Connection Status -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Source Connection -->
                <ui:Card Grid.Column="0" Margin="0,0,10,0">
                    <Grid Margin="20,15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                            <ui:SymbolIcon Symbol="Settings24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Source vCenter" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Row="1">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <Ellipse Width="12" Height="12" 
                                         Fill="{Binding IsSourceConnected, Converter={StaticResource BoolToColorConverter}}"
                                         Margin="0,0,8,0" 
                                         VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding SourceConnectionStatus}" 
                                           FontWeight="Medium"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                            
                            <ui:Button Content="Load Admin Configuration"
                                      Icon="{ui:SymbolIcon CloudArrowDown24}"
                                      Command="{Binding LoadSourceAdminConfigCommand}"
                                      IsEnabled="{Binding IsSourceConnected}"
                                      Appearance="Primary"
                                      Margin="0,10,0,0"
                                      HorizontalAlignment="Stretch"/>
                                      
                            <TextBlock Text="{Binding SourceDataStatus}"
                                      FontSize="12"
                                      Opacity="0.7"
                                      Margin="0,5,0,0"/>
                        </StackPanel>
                    </Grid>
                </ui:Card>

                <!-- Connection Arrow -->
                <ui:SymbolIcon Grid.Column="1" 
                              Symbol="ArrowRight24" 
                              FontSize="24"
                              VerticalAlignment="Center"
                              Margin="15,0"
                              Opacity="0.6"/>

                <!-- Target Connection -->
                <ui:Card Grid.Column="2" Margin="10,0,0,0">
                    <Grid Margin="20,15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                            <ui:SymbolIcon Symbol="Settings24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Target vCenter" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Row="1">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <Ellipse Width="12" Height="12" 
                                         Fill="{Binding IsTargetConnected, Converter={StaticResource BoolToColorConverter}}"
                                         Margin="0,0,8,0" 
                                         VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding TargetConnectionStatus}" 
                                           FontWeight="Medium"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                            
                            <ui:Button Content="Load Admin Configuration"
                                      Icon="{ui:SymbolIcon CloudArrowUp24}"
                                      Command="{Binding LoadTargetAdminConfigCommand}"
                                      IsEnabled="{Binding IsTargetConnected}"
                                      Appearance="Primary"
                                      Margin="0,10,0,0"
                                      HorizontalAlignment="Stretch"/>
                                      
                            <TextBlock Text="{Binding TargetDataStatus}"
                                      FontSize="12"
                                      Opacity="0.7"
                                      Margin="0,5,0,0"/>
                        </StackPanel>
                    </Grid>
                </ui:Card>
            </Grid>

            <!-- Admin Config Data Display -->
            <ui:Card Grid.Row="2" Margin="0,0,0,20">
                <Grid Margin="20,15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" 
                              Text="Administrative Configuration Overview" 
                              FontSize="16" 
                              FontWeight="SemiBold"
                              Margin="0,0,0,15"/>
                    
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Source Config Stats -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Source Configuration" FontWeight="Medium" Margin="0,0,0,10"/>
                            
                            <!-- Roles -->
                            <Border BorderBrush="{DynamicResource ControlElevationBorderBrush}" 
                                   BorderThickness="1" 
                                   CornerRadius="4" 
                                   Padding="12,8" 
                                   Margin="0,0,0,6"
                                   Background="{DynamicResource ControlFillColorDefaultBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <ui:SymbolIcon Grid.Column="0" Symbol="Person24" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding SourceRoles.Count, StringFormat='Roles ({0})'}" VerticalAlignment="Center" FontSize="12" FontWeight="Medium"/>
                                    <ui:Button Grid.Column="2" 
                                              Icon="{ui:SymbolIcon ArrowExport24}" 
                                              ToolTip="Export Roles"
                                              Command="{Binding ExportRolesCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"
                                              IsEnabled="{Binding SourceRoles.Count, Converter={StaticResource CountToVisibilityConverter}}"/>
                                    <ui:Button Grid.Column="3" 
                                              Icon="{ui:SymbolIcon ArrowImport24}" 
                                              ToolTip="Import Roles"
                                              Command="{Binding ImportRolesCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"/>
                                </Grid>
                            </Border>
                            
                            <!-- Permissions -->
                            <Border BorderBrush="{DynamicResource ControlElevationBorderBrush}" 
                                   BorderThickness="1" 
                                   CornerRadius="4" 
                                   Padding="12,8" 
                                   Margin="0,0,0,6"
                                   Background="{DynamicResource ControlFillColorDefaultBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <ui:SymbolIcon Grid.Column="0" Symbol="Shield24" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding SourcePermissions.Count, StringFormat='Permissions ({0})'}" VerticalAlignment="Center" FontSize="12" FontWeight="Medium"/>
                                    <ui:Button Grid.Column="2" 
                                              Icon="{ui:SymbolIcon ArrowExport24}" 
                                              ToolTip="Export Permissions"
                                              Command="{Binding ExportPermissionsCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"
                                              IsEnabled="{Binding SourcePermissions.Count, Converter={StaticResource CountToVisibilityConverter}}"/>
                                    <ui:Button Grid.Column="3" 
                                              Icon="{ui:SymbolIcon ArrowImport24}" 
                                              ToolTip="Import Permissions"
                                              Command="{Binding ImportPermissionsCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"/>
                                </Grid>
                            </Border>
                            
                            <!-- Folders -->
                            <Border BorderBrush="{DynamicResource ControlElevationBorderBrush}" 
                                   BorderThickness="1" 
                                   CornerRadius="4" 
                                   Padding="12,8" 
                                   Margin="0,0,0,6"
                                   Background="{DynamicResource ControlFillColorDefaultBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <ui:SymbolIcon Grid.Column="0" Symbol="Folder24" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding SourceFolders.Count, StringFormat='Folders ({0})'}" VerticalAlignment="Center" FontSize="12" FontWeight="Medium"/>
                                    <ui:Button Grid.Column="2" 
                                              Icon="{ui:SymbolIcon ArrowExport24}" 
                                              ToolTip="Export Folders"
                                              Command="{Binding ExportFoldersCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"
                                              IsEnabled="{Binding SourceFolders.Count, Converter={StaticResource CountToVisibilityConverter}}"/>
                                    <ui:Button Grid.Column="3" 
                                              Icon="{ui:SymbolIcon ArrowImport24}" 
                                              ToolTip="Import Folders"
                                              Command="{Binding ImportFoldersCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"/>
                                </Grid>
                            </Border>
                            
                            <!-- Tags -->
                            <Border BorderBrush="{DynamicResource ControlElevationBorderBrush}" 
                                   BorderThickness="1" 
                                   CornerRadius="4" 
                                   Padding="12,8" 
                                   Margin="0,0,0,6"
                                   Background="{DynamicResource ControlFillColorDefaultBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <ui:SymbolIcon Grid.Column="0" Symbol="Tag24" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding SourceTags.Count, StringFormat='Tags ({0})'}" VerticalAlignment="Center" FontSize="12" FontWeight="Medium"/>
                                    <ui:Button Grid.Column="2" 
                                              Icon="{ui:SymbolIcon ArrowExport24}" 
                                              ToolTip="Export Tags"
                                              Command="{Binding ExportTagsCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"
                                              IsEnabled="{Binding SourceTags.Count, Converter={StaticResource CountToVisibilityConverter}}"/>
                                    <ui:Button Grid.Column="3" 
                                              Icon="{ui:SymbolIcon ArrowImport24}" 
                                              ToolTip="Import Tags"
                                              Command="{Binding ImportTagsCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"/>
                                </Grid>
                            </Border>
                            
                            <!-- Certificates -->
                            <Border BorderBrush="{DynamicResource ControlElevationBorderBrush}" 
                                   BorderThickness="1" 
                                   CornerRadius="4" 
                                   Padding="12,8" 
                                   Margin="0,0,0,6"
                                   Background="{DynamicResource ControlFillColorDefaultBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <ui:SymbolIcon Grid.Column="0" Symbol="Certificate24" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding SourceCertificates.Count, StringFormat='Certificates ({0})'}" VerticalAlignment="Center" FontSize="12" FontWeight="Medium"/>
                                    <ui:Button Grid.Column="2" 
                                              Icon="{ui:SymbolIcon ArrowExport24}" 
                                              ToolTip="Export Certificates"
                                              Command="{Binding ExportCertificatesCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"
                                              IsEnabled="{Binding SourceCertificates.Count, Converter={StaticResource CountToVisibilityConverter}}"/>
                                    <ui:Button Grid.Column="3" 
                                              Icon="{ui:SymbolIcon ArrowImport24}" 
                                              ToolTip="Import Certificates"
                                              Command="{Binding ImportCertificatesCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"/>
                                </Grid>
                            </Border>
                            
                            <!-- Custom Attributes -->
                            <Border BorderBrush="{DynamicResource ControlElevationBorderBrush}" 
                                   BorderThickness="1" 
                                   CornerRadius="4" 
                                   Padding="12,8" 
                                   Margin="0,0,0,6"
                                   Background="{DynamicResource ControlFillColorDefaultBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <ui:SymbolIcon Grid.Column="0" Symbol="Settings24" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding SourceCustomAttributes.Count, StringFormat='Custom Attributes ({0})'}" VerticalAlignment="Center" FontSize="12" FontWeight="Medium"/>
                                    <ui:Button Grid.Column="2" 
                                              Icon="{ui:SymbolIcon ArrowExport24}" 
                                              ToolTip="Export Custom Attributes"
                                              Command="{Binding ExportCustomAttributesCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"
                                              IsEnabled="{Binding SourceCustomAttributes.Count, Converter={StaticResource CountToVisibilityConverter}}"/>
                                    <ui:Button Grid.Column="3" 
                                              Icon="{ui:SymbolIcon ArrowImport24}" 
                                              ToolTip="Import Custom Attributes"
                                              Command="{Binding ImportCustomAttributesCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                        
                        <ui:SymbolIcon Grid.Column="1" 
                                      Symbol="ArrowRight24" 
                                      FontSize="20"
                                      VerticalAlignment="Center"
                                      Margin="20,0"
                                      Opacity="0.6"/>
                        
                        <!-- Target Config Stats -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Target Configuration" FontWeight="Medium" Margin="0,0,0,10"/>
                            
                            <!-- Roles -->
                            <Border BorderBrush="{DynamicResource ControlElevationBorderBrush}" 
                                   BorderThickness="1" 
                                   CornerRadius="4" 
                                   Padding="12,8" 
                                   Margin="0,0,0,6"
                                   Background="{DynamicResource ControlFillColorDefaultBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <ui:SymbolIcon Grid.Column="0" Symbol="Person24" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding TargetRoles.Count, StringFormat='Roles ({0})'}" VerticalAlignment="Center" FontSize="12" FontWeight="Medium"/>
                                    <ui:Button Grid.Column="2" 
                                              Icon="{ui:SymbolIcon ArrowExport24}" 
                                              ToolTip="Export Roles from Target"
                                              Command="{Binding ExportTargetRolesCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"
                                              IsEnabled="{Binding TargetRoles.Count, Converter={StaticResource CountToVisibilityConverter}}"/>
                                </Grid>
                            </Border>
                            
                            <!-- Permissions -->
                            <Border BorderBrush="{DynamicResource ControlElevationBorderBrush}" 
                                   BorderThickness="1" 
                                   CornerRadius="4" 
                                   Padding="12,8" 
                                   Margin="0,0,0,6"
                                   Background="{DynamicResource ControlFillColorDefaultBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <ui:SymbolIcon Grid.Column="0" Symbol="Shield24" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding TargetPermissions.Count, StringFormat='Permissions ({0})'}" VerticalAlignment="Center" FontSize="12" FontWeight="Medium"/>
                                    <ui:Button Grid.Column="2" 
                                              Icon="{ui:SymbolIcon ArrowExport24}" 
                                              ToolTip="Export Permissions from Target"
                                              Command="{Binding ExportTargetPermissionsCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"
                                              IsEnabled="{Binding TargetPermissions.Count, Converter={StaticResource CountToVisibilityConverter}}"/>
                                </Grid>
                            </Border>
                            
                            <!-- Folders -->
                            <Border BorderBrush="{DynamicResource ControlElevationBorderBrush}" 
                                   BorderThickness="1" 
                                   CornerRadius="4" 
                                   Padding="12,8" 
                                   Margin="0,0,0,6"
                                   Background="{DynamicResource ControlFillColorDefaultBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <ui:SymbolIcon Grid.Column="0" Symbol="Folder24" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding TargetFolders.Count, StringFormat='Folders ({0})'}" VerticalAlignment="Center" FontSize="12" FontWeight="Medium"/>
                                    <ui:Button Grid.Column="2" 
                                              Icon="{ui:SymbolIcon ArrowExport24}" 
                                              ToolTip="Export Folders from Target"
                                              Command="{Binding ExportTargetFoldersCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"
                                              IsEnabled="{Binding TargetFolders.Count, Converter={StaticResource CountToVisibilityConverter}}"/>
                                </Grid>
                            </Border>
                            
                            <!-- Tags -->
                            <Border BorderBrush="{DynamicResource ControlElevationBorderBrush}" 
                                   BorderThickness="1" 
                                   CornerRadius="4" 
                                   Padding="12,8" 
                                   Margin="0,0,0,6"
                                   Background="{DynamicResource ControlFillColorDefaultBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <ui:SymbolIcon Grid.Column="0" Symbol="Tag24" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding TargetTags.Count, StringFormat='Tags ({0})'}" VerticalAlignment="Center" FontSize="12" FontWeight="Medium"/>
                                    <ui:Button Grid.Column="2" 
                                              Icon="{ui:SymbolIcon ArrowExport24}" 
                                              ToolTip="Export Tags from Target"
                                              Command="{Binding ExportTargetTagsCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"
                                              IsEnabled="{Binding TargetTags.Count, Converter={StaticResource CountToVisibilityConverter}}"/>
                                </Grid>
                            </Border>
                            
                            <!-- Certificates -->
                            <Border BorderBrush="{DynamicResource ControlElevationBorderBrush}" 
                                   BorderThickness="1" 
                                   CornerRadius="4" 
                                   Padding="12,8" 
                                   Margin="0,0,0,6"
                                   Background="{DynamicResource ControlFillColorDefaultBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <ui:SymbolIcon Grid.Column="0" Symbol="Certificate24" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding TargetCertificates.Count, StringFormat='Certificates ({0})'}" VerticalAlignment="Center" FontSize="12" FontWeight="Medium"/>
                                    <ui:Button Grid.Column="2" 
                                              Icon="{ui:SymbolIcon ArrowExport24}" 
                                              ToolTip="Export Certificates from Target"
                                              Command="{Binding ExportTargetCertificatesCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"
                                              IsEnabled="{Binding TargetCertificates.Count, Converter={StaticResource CountToVisibilityConverter}}"/>
                                </Grid>
                            </Border>
                            
                            <!-- Custom Attributes -->
                            <Border BorderBrush="{DynamicResource ControlElevationBorderBrush}" 
                                   BorderThickness="1" 
                                   CornerRadius="4" 
                                   Padding="12,8" 
                                   Margin="0,0,0,6"
                                   Background="{DynamicResource ControlFillColorDefaultBrush}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <ui:SymbolIcon Grid.Column="0" Symbol="Settings24" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Grid.Column="1" Text="{Binding TargetCustomAttributes.Count, StringFormat='Custom Attributes ({0})'}" VerticalAlignment="Center" FontSize="12" FontWeight="Medium"/>
                                    <ui:Button Grid.Column="2" 
                                              Icon="{ui:SymbolIcon ArrowExport24}" 
                                              ToolTip="Export Custom Attributes from Target"
                                              Command="{Binding ExportTargetCustomAttributesCommand}"
                                              Appearance="Secondary"
                                              FontSize="10"
                                              Padding="6,4"
                                              Margin="4,0,0,0"
                                              IsEnabled="{Binding TargetCustomAttributes.Count, Converter={StaticResource CountToVisibilityConverter}}"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Grid>
            </ui:Card>

            <!-- Migration Configuration -->
            <ui:Card Grid.Row="3" Margin="0,0,0,20">
                <Grid Margin="20,15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" 
                              Text="Migration Configuration" 
                              FontSize="16" 
                              FontWeight="SemiBold"
                              Margin="0,0,0,15"/>
                    
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Migration Options -->
                        <StackPanel Grid.Column="0" Margin="0,0,20,0">
                            <TextBlock Text="Migration Options" FontWeight="Medium" Margin="0,0,0,10"/>
                            
                            <CheckBox Content="Migrate Roles" 
                                     IsChecked="{Binding MigrateRoles}"
                                     Margin="0,0,0,5"/>
                            
                            <CheckBox Content="Migrate Permissions" 
                                     IsChecked="{Binding MigratePermissions}"
                                     Margin="0,0,0,5"/>
                            
                            <CheckBox Content="Migrate Folders" 
                                     IsChecked="{Binding MigrateFolders}"
                                     Margin="0,0,0,5"/>
                            
                            <CheckBox Content="Migrate Tags" 
                                     IsChecked="{Binding MigrateTags}"
                                     Margin="0,0,0,5"/>
                            
                            <CheckBox Content="Migrate Certificates" 
                                     IsChecked="{Binding MigrateCertificates}"
                                     Margin="0,0,0,5"/>
                            
                            <CheckBox Content="Migrate Custom Attributes" 
                                     IsChecked="{Binding MigrateCustomAttributes}"
                                     Margin="0,0,0,5"/>
                            
                            <CheckBox Content="Validation only (no changes)" 
                                     IsChecked="{Binding ValidateOnly}"
                                     Margin="0,0,0,5"/>
                        </StackPanel>
                        
                        <!-- Migration Actions -->
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Migration Actions" FontWeight="Medium" Margin="0,0,0,10"/>
                            
                            <ui:Button Content="Validate Migration"
                                      Icon="{ui:SymbolIcon CheckmarkCircle24}"
                                      Command="{Binding ValidateMigrationCommand}"
                                      IsEnabled="{Binding CanValidateMigration}"
                                      Margin="0,0,0,10"
                                      HorizontalAlignment="Stretch"/>
                            
                            <ui:Button Content="Start Admin Config Migration"
                                      Icon="{ui:SymbolIcon Play24}"
                                      Command="{Binding StartMigrationCommand}"
                                      IsEnabled="{Binding CanStartMigration}"
                                      Appearance="Primary"
                                      HorizontalAlignment="Stretch"/>
                            
                            <ProgressBar Value="{Binding MigrationProgress}"
                                        Maximum="100"
                                        Height="6"
                                        Margin="0,15,0,5"
                                        Visibility="{Binding IsMigrationInProgress, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            
                            <TextBlock Text="{Binding MigrationStatus}"
                                      FontSize="12"
                                      TextWrapping="Wrap"
                                      Margin="0,5,0,0"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </ui:Card>

            <!-- Activity Log -->
            <ui:Card Grid.Row="4">
                <Grid Margin="20,15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" 
                              Text="Activity Log" 
                              FontSize="16" 
                              FontWeight="SemiBold"
                              Margin="0,0,0,15"/>
                    
                    <ScrollViewer Grid.Row="1"
                                 Height="300" 
                                 VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto"
                                 PanningMode="Both"
                                 CanContentScroll="False">
                        <TextBox Text="{Binding ActivityLog}"
                                IsReadOnly="True"
                                Background="Transparent"
                                BorderThickness="0"
                                TextWrapping="Wrap"
                                FontFamily="Consolas"
                                FontSize="12"/>
                    </ScrollViewer>
                </Grid>
            </ui:Card>
        </Grid>
    </ScrollViewer>
</Page>
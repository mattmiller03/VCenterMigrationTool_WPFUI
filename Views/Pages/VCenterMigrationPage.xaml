<Page x:Class="VCenterMigrationTool.Views.Pages.VCenterMigrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:viewmodels="clr-namespace:VCenterMigrationTool.ViewModels"
      xmlns:helpers="clr-namespace:VCenterMigrationTool.Helpers"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance Type=viewmodels:VCenterMigrationViewModel}"
      Title="VCenterMigrationPage">
    <Page.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <helpers:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        <helpers:BoolToParameterConverter x:Key="BoolToParameterConverter" />
    </Page.Resources>
    <Grid Margin="42">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="200" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" FontSize="24" FontWeight="Medium" Text="vCenter Object Migration" />
        <Grid Grid.Row="1" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>
            <ui:CardControl Grid.Column="0">
                <StackPanel>
                    <TextBlock Text="Select Objects to Migrate" FontWeight="Medium" />
                    <CheckBox IsChecked="{Binding MigrateRoles}" Content="Roles and Permissions" Margin="0,10,0,0" />
                    <CheckBox IsChecked="{Binding MigrateFolders}" Content="VM and Template Folders" Margin="0,5,0,0" />
                    <CheckBox IsChecked="{Binding MigrateTags}" Content="Tags and Categories" Margin="0,5,0,0" />
                    <TextBlock Text="Cluster-Specific Objects" FontWeight="Medium" Margin="0,20,0,5" />
                    <ComboBox ItemsSource="{Binding SourceClusters}" SelectedItem="{Binding SelectedSourceCluster}" DisplayMemberPath="Name" />
                    <TextBlock Visibility="{Binding IsLoadingClusterItems, Converter={StaticResource BoolToVisibilityConverter}}" Text="Loading items..." Margin="0,5,0,0"/>
                    <ListView ItemsSource="{Binding ClusterItems}" Margin="0,10,0,0" MaxHeight="200">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsSelected}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}"/>
                                <GridViewColumn Header="Type" DisplayMemberBinding="{Binding Type}" Width="100" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                </StackPanel>
            </ui:CardControl>
            <ui:CardControl Grid.Column="2">
                <StackPanel>
                    <TextBlock Text="Migration Status" FontWeight="Medium" />
                    <ui:Button Command="{Binding StartMigrationCommand}" Content="Start Migration" Appearance="Primary" Margin="0,10,0,0" IsEnabled="{Binding IsMigrating, Converter={StaticResource BoolToParameterConverter}, ConverterParameter=inverse}" />
                    <DataGrid ItemsSource="{Binding MigrationTasks}" AutoGenerateColumns="False" Margin="0,10,0,0" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Object Name" Binding="{Binding ObjectName}" Width="*" />
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" />
                            <DataGridTemplateColumn Header="Progress" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ProgressBar Value="{Binding Progress}" Maximum="100" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </ui:CardControl>
        </Grid>
        <StackPanel Grid.Row="2" Margin="0,20,0,10">
            <TextBlock Text="{Binding OverallStatus}" FontWeight="Medium" />
            <ProgressBar Value="{Binding OverallProgress}" Maximum="100" Margin="0,5,0,0" />
        </StackPanel>
        <ui:TextBox Grid.Row="3" Text="{Binding CurrentTaskDetails}" IsReadOnly="True" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" FontFamily="Consolas"/>
    </Grid>
</Page>
<Page
    x:Class="VCenterMigrationTool.Views.Pages.ResourcePoolMigrationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:VCenterMigrationTool.ViewModels"
    xmlns:helpers="clr-namespace:VCenterMigrationTool.Helpers"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewmodels:ResourcePoolMigrationViewModel, IsDesignTimeCreatable=False}"
    Title="ResourcePoolMigrationPage"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Page.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <helpers:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
        <helpers:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>
        <helpers:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <helpers:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Disabled"
                  PanningMode="VerticalOnly"
                  CanContentScroll="True">
        <Grid Margin="42">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*" MinHeight="400"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Resource Pool Migration" 
                               FontSize="28" 
                               FontWeight="SemiBold"/>
                    <TextBlock Text="Migrate resource pools and their configurations between vCenter clusters" 
                               Opacity="0.7"
                               FontSize="14"
                               Margin="0,5,0,0"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <ui:Button Grid.Column="1" 
                          Content="Refresh" 
                          Icon="{ui:SymbolIcon ArrowSync24}"
                          Command="{Binding ConnectToVCentersCommand}"
                          Appearance="Secondary"
                          VerticalAlignment="Center"
                          Padding="15,8"/>
            </Grid>

            <!-- Connection Status -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Source Connection -->
                <ui:Card Grid.Column="0" Margin="0,0,10,0">
                    <Grid Margin="20,15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                            <ui:SymbolIcon Symbol="Server24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Source vCenter" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Row="1">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <Ellipse Width="12" Height="12" 
                                         Fill="{Binding IsConnected, Converter={StaticResource BoolToColorConverter}}"
                                         Margin="0,0,8,0" 
                                         VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding ConnectionStatus}" 
                                           FontWeight="Medium"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                            
                            <ui:Button Content="Load Source Clusters"
                                      Icon="{ui:SymbolIcon CloudArrowDown24}"
                                      Command="{Binding LoadSourceClustersCommand}"
                                      IsEnabled="{Binding IsConnected}"
                                      Appearance="Primary"
                                      Margin="0,10,0,0"
                                      HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Grid>
                </ui:Card>

                <!-- Arrow -->
                <ui:SymbolIcon Grid.Column="1" 
                              Symbol="ArrowRight24" 
                              FontSize="24" 
                              VerticalAlignment="Center"
                              Margin="10,0"/>

                <!-- Target Connection -->
                <ui:Card Grid.Column="2" Margin="10,0,0,0">
                    <Grid Margin="20,15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                            <ui:SymbolIcon Symbol="Server24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Target vCenter" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <StackPanel Grid.Row="1">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <Ellipse Width="12" Height="12" 
                                         Fill="{Binding IsConnected, Converter={StaticResource BoolToColorConverter}}"
                                         Margin="0,0,8,0" 
                                         VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding ConnectionStatus}" 
                                           FontWeight="Medium"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                            
                            <ui:Button Content="Load Target Clusters"
                                      Icon="{ui:SymbolIcon CloudArrowDown24}"
                                      Command="{Binding LoadTargetClustersCommand}"
                                      IsEnabled="{Binding IsConnected}"
                                      Appearance="Primary"
                                      Margin="0,10,0,0"
                                      HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Grid>
                </ui:Card>
            </Grid>

            <!-- Migration Options -->
            <ui:Card Grid.Row="2" Margin="0,0,0,20">
                <Grid Margin="20,15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                        <ui:SymbolIcon Symbol="Options24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Migration Options" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <CheckBox Grid.Column="0" 
                                 IsChecked="{Binding PreserveSettings}"
                                 Margin="0,0,20,0">
                            <StackPanel>
                                <TextBlock Text="Preserve Settings" FontWeight="Medium"/>
                                <TextBlock Text="Keep CPU/Memory allocations" 
                                          FontSize="11" 
                                          Opacity="0.7"
                                          TextWrapping="Wrap"/>
                            </StackPanel>
                        </CheckBox>

                        <CheckBox Grid.Column="1" 
                                 IsChecked="{Binding MigrateChildPools}"
                                 Margin="0,0,20,0">
                            <StackPanel>
                                <TextBlock Text="Include Child Pools" FontWeight="Medium"/>
                                <TextBlock Text="Migrate nested resource pools" 
                                          FontSize="11" 
                                          Opacity="0.7"
                                          TextWrapping="Wrap"/>
                            </StackPanel>
                        </CheckBox>

                        <CheckBox Grid.Column="2" 
                                 IsChecked="{Binding ValidateBeforeMigration}"
                                 Margin="0,0,20,0">
                            <StackPanel>
                                <TextBlock Text="Validate First" FontWeight="Medium"/>
                                <TextBlock Text="Check compatibility before migration" 
                                          FontSize="11" 
                                          Opacity="0.7"
                                          TextWrapping="Wrap"/>
                            </StackPanel>
                        </CheckBox>

                        <CheckBox Grid.Column="3" 
                                 IsChecked="{Binding MoveVMsToResourcePools}">
                            <StackPanel>
                                <TextBlock Text="Move VMs" FontWeight="Medium"/>
                                <TextBlock Text="Assign VMs to migrated pools" 
                                          FontSize="11" 
                                          Opacity="0.7"
                                          TextWrapping="Wrap"/>
                            </StackPanel>
                        </CheckBox>
                    </Grid>
                </Grid>
            </ui:Card>

            <!-- Cluster and Resource Pool Selection -->
            <Grid Grid.Row="3" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Source Cluster and Pools -->
                <ui:Card Grid.Column="0">
                    <Grid Margin="20,15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*" MaxHeight="300"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                            <ui:SymbolIcon Symbol="FolderOpen24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Source Cluster" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center"/>
                        </StackPanel>

                        <ComboBox Grid.Row="1" 
                                  ItemsSource="{Binding SourceClusters}"
                                  SelectedItem="{Binding SelectedSourceCluster}"
                                  DisplayMemberPath="Name"
                                  Margin="0,0,0,15"
                                  HorizontalAlignment="Stretch">
                            <ComboBox.ItemContainerStyle>
                                <Style TargetType="ComboBoxItem">
                                    <Setter Property="ToolTip">
                                        <Setter.Value>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                                <TextBlock Text="{Binding HostCount, StringFormat='Hosts: {0}'}"/>
                                                <TextBlock Text="{Binding VmCount, StringFormat='VMs: {0}'}"/>
                                            </StackPanel>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ComboBox.ItemContainerStyle>
                        </ComboBox>

                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBlock Text="Resource Pools" FontWeight="Medium" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding SelectedSourcePools.Count, StringFormat=' ({0} selected)'}" 
                                              Opacity="0.7" VerticalAlignment="Center"/>
                                    <ui:Button Content="Load Pools"
                                              Icon="{ui:SymbolIcon ArrowDownload24}"
                                              Command="{Binding LoadSourceResourcePoolsCommand}"
                                              Appearance="Secondary"
                                              FontSize="12"
                                              Padding="8,4"
                                              Margin="10,0,0,0"/>
                                </StackPanel>

                                <ItemsControl ItemsSource="{Binding SourceResourcePools}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{DynamicResource ControlFillColorDefaultBrush}" 
                                                   CornerRadius="4" 
                                                   Padding="10"
                                                   Margin="0,0,0,5">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <CheckBox Grid.Column="0" 
                                                             IsChecked="{Binding IsSelected}"
                                                             VerticalAlignment="Center"
                                                             Margin="0,0,10,0"/>

                                                    <StackPanel Grid.Column="1">
                                                        <TextBlock Text="{Binding Name}" FontWeight="Medium"/>
                                                        <TextBlock Text="{Binding VmInfo}" 
                                                                  FontSize="11" 
                                                                  Opacity="0.7"/>
                                                    </StackPanel>

                                                    <ui:InfoBadge Grid.Column="2" 
                                                                 Value="{Binding VmCount}"
                                                                 VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </ui:Card>

                <!-- Target Cluster and Pools -->
                <ui:Card Grid.Column="2">
                    <Grid Margin="20,15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*" MaxHeight="300"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                            <ui:SymbolIcon Symbol="FolderAdd24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Target Cluster" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center"/>
                        </StackPanel>

                        <ComboBox Grid.Row="1" 
                                  ItemsSource="{Binding TargetClusters}"
                                  SelectedItem="{Binding SelectedTargetCluster}"
                                  DisplayMemberPath="Name"
                                  Margin="0,0,0,15"
                                  HorizontalAlignment="Stretch">
                            <ComboBox.ItemContainerStyle>
                                <Style TargetType="ComboBoxItem">
                                    <Setter Property="ToolTip">
                                        <Setter.Value>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                                <TextBlock Text="{Binding HostCount, StringFormat='Hosts: {0}'}"/>
                                                <TextBlock Text="{Binding VmCount, StringFormat='VMs: {0}'}"/>
                                            </StackPanel>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ComboBox.ItemContainerStyle>
                        </ComboBox>

                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBlock Text="Existing Pools" FontWeight="Medium" VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding TargetResourcePools.Count, StringFormat=' ({0} found)'}" 
                                              Opacity="0.7" VerticalAlignment="Center"/>
                                    <ui:Button Content="Load Pools"
                                              Icon="{ui:SymbolIcon ArrowDownload24}"
                                              Command="{Binding LoadTargetResourcePoolsCommand}"
                                              Appearance="Secondary"
                                              FontSize="12"
                                              Padding="8,4"
                                              Margin="10,0,0,0"/>
                                </StackPanel>

                                <ItemsControl ItemsSource="{Binding TargetResourcePools}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="{DynamicResource ControlFillColorDefaultBrush}" 
                                                   CornerRadius="4" 
                                                   Padding="10"
                                                   Margin="0,0,0,5">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>

                                                    <StackPanel Grid.Column="0">
                                                        <TextBlock Text="{Binding Name}" FontWeight="Medium"/>
                                                        <TextBlock Text="{Binding VmInfo}" 
                                                                  FontSize="11" 
                                                                  Opacity="0.7"/>
                                                    </StackPanel>

                                                    <ui:InfoBadge Grid.Column="1" 
                                                                 Value="{Binding VmCount}"
                                                                 VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </ui:Card>
            </Grid>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <ui:Button Content="Export Configuration" 
                          Icon="{ui:SymbolIcon DocumentArrowLeft24}"
                          Command="{Binding ExportConfigurationCommand}"
                          Appearance="Secondary"
                          Margin="0,0,10,0"
                          Padding="15,10"/>
                
                <ui:Button Content="Import Configuration" 
                          Icon="{ui:SymbolIcon DocumentArrowRight24}"
                          Command="{Binding ImportConfigurationCommand}"
                          Appearance="Secondary"
                          Margin="0,0,10,0"
                          Padding="15,10"/>
                
                <ui:Button Content="Validate Migration" 
                          Icon="{ui:SymbolIcon CheckmarkCircle24}"
                          Command="{Binding ValidateMigrationCommand}"
                          IsEnabled="{Binding SelectedSourcePools.Count, Converter={StaticResource CountToVisibilityConverter}}"
                          Appearance="Info"
                          Margin="0,0,10,0"
                          Padding="15,10"/>
                
                <ui:Button Content="Start Migration" 
                          Icon="{ui:SymbolIcon ArrowForward24}"
                          Command="{Binding StartMigrationCommand}"
                          IsEnabled="{Binding SelectedSourcePools.Count, Converter={StaticResource CountToVisibilityConverter}}"
                          Appearance="Primary"
                          Padding="15,10"/>
            </StackPanel>

            <!-- Activity Log -->
            <ui:Card Grid.Row="5">
                <Grid Margin="20,15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                        <ui:SymbolIcon Symbol="DocumentBulletList24" Margin="0,0,10,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Activity Log" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center"/>
                        <ui:Button Content="Clear"
                                  Icon="{ui:SymbolIcon Delete24}"
                                  Command="{Binding ClearLogCommand}"
                                  Appearance="Secondary"
                                  FontSize="12"
                                  Padding="8,4"
                                  Margin="20,0,0,0"/>
                    </StackPanel>

                    <Border Grid.Row="1" 
                           Background="{DynamicResource ControlFillColorDefaultBrush}"
                           CornerRadius="4"
                           Padding="10">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <TextBox Text="{Binding LogOutput}"
                                    IsReadOnly="True"
                                    BorderThickness="0"
                                    Background="Transparent"
                                    FontFamily="Cascadia Code,Consolas,Courier New"
                                    FontSize="12"
                                    TextWrapping="Wrap"/>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </ui:Card>

            <!-- Progress Overlay -->
            <Grid Grid.Row="0" Grid.RowSpan="6" 
                  Background="{DynamicResource ApplicationBackgroundBrush}"
                  Opacity="0.95"
                  Visibility="{Binding IsMigrationInProgress, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <ui:ProgressRing Width="60" Height="60" IsIndeterminate="True"/>
                    <TextBlock Text="{Binding MigrationProgress}" 
                              FontSize="16" 
                              FontWeight="Medium"
                              Margin="0,20,0,0"
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding OperationStatus}" 
                              FontSize="14" 
                              Opacity="0.7"
                              Margin="0,10,0,0"
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>
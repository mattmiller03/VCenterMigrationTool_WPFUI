<Page
    x:Class="VCenterMigrationTool.Views.Pages.ConnectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:VCenterMigrationTool.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewmodels:ConnectionViewModel, IsDesignTimeCreatable=False}"
    Title="ConnectionPage"
    d:DesignHeight="450" d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Grid Margin="42">
        <StackPanel MaxWidth="400">
            <TextBlock FontSize="24" FontWeight="Medium" Text="vCenter Connection" Margin="0,0,0,20"/>

            <TextBlock Text="Select a Connection Profile" Margin="0,0,0,5"/>
            <ComboBox 
                ItemsSource="{Binding ConnectionProfiles}"
                DisplayMemberPath="Name"
                SelectedItem="{Binding SelectedProfile}"
                Margin="0,0,0,15"/>

            <TextBlock Text="vCenter Server Address" Margin="0,0,0,5"/>
            <ui:TextBox Text="{Binding SelectedProfile.ServerAddress}" Margin="0,0,0,15"/>

            <TextBlock Text="Username" Margin="0,0,0,5"/>
            <ui:TextBox Text="{Binding SelectedProfile.Username}" Margin="0,0,0,15"/>

            <TextBlock Text="Password" Margin="0,0,0,5"/>
            <ui:PasswordBox x:Name="PasswordBox" Margin="0,0,0,20"/>

            <ui:Button 
                Content="Connect" 
                Appearance="Primary" 
                Command="{Binding ConnectCommand}"
                CommandParameter="{Binding ElementName=PasswordBox}"/>

            <TextBlock Text="{Binding ConnectionStatus}" Margin="0,20,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
        </StackPanel>
    </Grid>
</Page>
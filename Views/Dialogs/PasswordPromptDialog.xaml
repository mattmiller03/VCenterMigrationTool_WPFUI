<ui:FluentWindow
    x:Class="VCenterMigrationTool.Views.Dialogs.PasswordPromptDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:viewmodels="clr-namespace:VCenterMigrationTool.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance viewmodels:PasswordPromptViewModel, IsDesignTimeCreatable=True}"
    Title="{Binding ViewModel.Title}"
    Height="220"
    Width="350"
    WindowStartupLocation="CenterOwner"
    ExtendsContentIntoTitleBar="True"
    WindowBackdropType="Mica">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding ViewModel.Message}" TextWrapping="Wrap" />

        <ui:PasswordBox
            x:Name="PasswordBox"
            Grid.Row="1"
            Margin="0,15,0,0"
            PlaceholderText="Password"  
            VerticalAlignment="Center"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <ui:Button Content="OK" IsDefault="True" Appearance="Primary" Click="OkButton_Click" />
            <ui:Button Content="Cancel" IsCancel="True" Margin="10,0,0,0" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>